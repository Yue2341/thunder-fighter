# 🔍 雷霆战机 · 项目分析与优化建议（复审版）

> 基于《项目总结.md》《使用说明.md》《趣味性优化建议.md》三份文档的学习，以及对 `index.html`（1449 行）与 `server.js`（264 行）实际代码的核对，给出的独立评估。更新时间：2026-09。

---

## 一、项目总结（我的理解）

### 1.1 项目是什么

**雷霆战机**是一个全栈单仓的 2D 飞行射击网页小游戏，核心亮点是**账号社交 + P2P 联机合作**，而不只是单机小游戏：

- **前端**：单文件 `index.html`，Canvas 2D 渲染 + Web Audio 实时合成音效/BGM，游戏引擎与全部 UI（登录/菜单/好友/房间/暂停/结算）一体，约 1450 行，零构建、零框架。
- **后端**：`server.js`，Express + ws 共约 264 行，承担账号/会话/战绩/好友/在线状态/邀请信令，并静态托管前端。数据用 `data.json` 文件持久化（运行时自动生成）。
- **联机架构**：游戏数据走 **PeerJS P2P**（WebRTC，云信令），自有后端只做"社交信令"。房主权威模拟 + 20Hz 快照下发，房客"发指令 + 本地预测渲染"——这是本项目最正确的架构决策：游戏数据不经过服务器，免费托管平台（Bonto）也能扛住。
- **部署**：Bonto 免费托管（https://thunder-fighter.bonto.run），GitHub 仓库 main 分支，经 Bonto MCP `upload_file` 一键更新；Render/Docker 配置备用。

### 1.2 完成度评价：一个小而完整的闭环

用一句话概括：**这是一个"能玩、能联机、能留存记录、能拉好友"的完整产品闭环**，而不是 demo。已完成的关键能力：

| 维度 | 已实现 | 完成度 |
|------|--------|--------|
| 玩法 | 移动/自动开火/炸弹、火力 1~5 级、3 种道具、4 类敌机、动态难度（小兵随时间增强 + Boss 多波递增）、BGM/音效/暂停 | ✅ 完整 |
| 联机 | 房主权威 + 快照同步、邀请对战全自动建房、房间码/邀请链接/输入码三种加入方式、联机音效同步 | ✅ 完整 |
| 社交 | 10 测试账户、登录会话（token + localStorage）、好友增删 + 实时在线状态、战绩近 5 局、全员排行榜 | ✅ 完整 |
| 工程 | 端口占用自愈、联机房客崩溃/无音效两个历史 Bug 已修、凭据已 gitignore | ✅ 基本健康 |

### 1.3 代码质量观察（核对代码后的发现）

**优点**
- 结构清晰：`index.html` 内模块分区明确（音频 → 实体类 → 游戏逻辑 → 联机 Net 对象 → UI/菜单流），`server.js` API 分区带注释，可读性好。
- 防御性到位：联机数据解析均有 try/catch 或判空；`Net.host()` 遇房间码撞车（unavailable-id）自动重建；端口占用自动顺延。
- 关键 bug 修复有据（`initView()` 改用 `new Player(1)` 解决房客崩溃），且文档记录了"勿回归"清单，这是很好的工程习惯。

**风险点（真实存在于代码中，非文档推测）**
1. **单文件已达 1450 行且持续增长**——按《趣味性优化建议.md》再上连击/金币/商店/成就，`index.html` 会突破 2500+ 行，可维护性快速恶化。**建议尽早拆分**：`game.js`（引擎）、`net.js`（联机）、`ui.js`（菜单/社交），HTML 只留骨架。改动成本低（纯静态引用），越晚拆越贵。
2. **安全是"测试品"级别**：密码明文存 `data.json` 与代码常量；token 只存内存 Map（重启全员掉线）；`/api/leaderboard`、`/api/accounts` 无鉴权；战绩上报无任何防作弊（可 POST 任意高分）。作为测试项目可接受，但若对外运营需至少：bcrypt 哈希、token 落盘或改 JWT、分数上报加服务端合理性校验。
3. **联机健壮性薄弱**：房主掉线房客直接结算（`onHostLeft`），无断线重连（PeerJS 房间码本可复用）；无心跳检测，半开连接只能靠 close 事件；20Hz 快照 `reliable: true` 全可靠传输，弱网时可能引入延迟积压（状态同步更适合部分不可靠通道或只发最新快照）。
4. **无自动化测试**：`server.js` 的 API 区逻辑简单但已有回归史（房间码加入补漏），连 10 行的 curl 冒烟脚本都没有。
5. **无移动端适配**：全部操作依赖鼠标/键盘，没有 touch 事件——而文档建议的"微信小游戏场景"恰恰是移动端。

### 1.4 与《趣味性优化建议.md》的对照

该文档的建议质量高：参考了雷霆战机（关卡+局外养成）、吸血鬼幸存者（build 分支）、合成类（进度感）三条已验证的设计规律，且给出了落地位置。我认可其**第一梯队（打击感四件套 + 连击 + Boss 警示）性价比最高**的排序——这些全部可纯前端实现，不碰后端，单文件改动可控。

我补充两点它未覆盖的盲区：
- **新手引导缺失**：所有建议都默认"已有玩家"，但当前游戏死亡即结束、无教学关卡，新玩家第一局可能 30 秒就死。留存的第一环是"首局体验"，一个 10 秒的操作提示（移动开火/炸弹时机）比成就系统的优先级更高。
- **死亡惩罚曲线**：当前 5 条命打完即终局，一局 2~5 分钟。对休闲玩家而言"一局归零"挫败感强，可考虑"续命金币"或阶段式复活，把单局拉长，再叠加连击系统才有发挥空间。

---

## 二、优化建议（按"先补地基，再上趣味"重排）

### 第一梯队：地基加固（在做任何新玩法前，1~2 天工作量）

| # | 建议 | 理由 | 落地方式 |
|---|------|------|----------|
| 1 | **拆分 index.html** | 1450 行单文件是后续一切迭代的瓶颈；趣味建议 80% 要改前端，先拆先受益 | 拆为 `game.js` / `net.js` / `ui.js` 三文件，HTML 引用；纯机械搬移，半天可完成并回归验证 |
| 2 | **移动端触屏** | 现有联机/邀请链路天然适合"发链接给手机好友"，但没有触屏等于自断一半场景；也是接住微信小游戏的前提 | canvas 加 touchmove 拖拽移动（手指上方偏移 60px 防遮挡），加虚拟炸弹按钮 |
| 3 | **服务端冒烟测试脚本** | 历史已有"房间码入口漏掉"类回归；每次 Bonto 部署前跑一遍防回归 | `test.sh`：curl 覆盖 登录→战绩→好友→排行榜 5 个接口断言，10 行搞定 |
| 4 | **API 最小安全加固** | 上线即有真实用户，明文密码与无鉴权排行榜是最先暴露的问题 | bcrypt 哈希 + `/api/accounts` 隐藏 UID 映射 + 分数合理性校验（如 score ≤ kills×均值×N） |

### 第二梯队：趣味性（采纳原建议文档第一梯队，并给出我的优先级调整）

1. **打击感四件套**（命中顿帧 + 受击白闪 + 分级震屏 + 得分飘字）——同意原文档排序，这是"玩起来像不像好游戏"的分水岭。补充：现有 `game.shake` 是单一强度，分级只需把 `shake=0.6`（炸弹）与命中 0.15/击杀 0.3/Boss 死亡 0.8 区分开，改动极小。
2. **连击 Combo + 音调递增**——同意。注意与"5 命归零"的死亡曲线配合：连击中断机制本身就给了玩家"保连击"的第二目标，能显著延长单局注意力。
3. **波次横幅 + Boss WARNING**——同意，且现有 `nextBossTime` 框架加提示零风险。
4. **我额外插入：新手引导/操作提示**——高于星级评价与商店（原文档第二梯队），因为没有它，前两个系统触达不了留存玩家。

### 第三梯队：局外系统（金币/商店/成就/星级）

方向认同，但建议**先加"每日首胜"这类零存档复杂度的**（只需 server.js 记 uid+日期），观察日活后再上完整金币经济——局外商店一旦上线，涉及存档迁移、数值平衡，是全项目最贵的单点投入，值得用数据验证需求后再做。

### 第四梯队：联机体验

- **断线重连（10s 内）**：原建议文档已列，我把它提到社交裂变之前——"打到一半掉线永久结算"是联机口碑杀手。
- **快照同步优化**：改"只发最新快照、可丢旧帧"（发送侧节流 + 序列号，接收侧丢弃过期帧），弱网体验立竿见影。

---

## 三、一句话结论

**这是一个架构决策正确（P2P 联机 + 轻后端）、完成度高、历史 bug 管理规范的小而全项目；当前最大的风险不是"不够好玩"，而是单文件体量与无移动端/无测试正在透支它的可迭代性。建议先花 1~2 天拆文件 + 加触屏 + 补冒烟测试，然后按原建议文档的打击感/连击路线快速迭代趣味性，局外养成系统用"每日首胜"低配探路。**

---

*本复审基于三份项目文档与源码实际核对（index.html 1449 行、server.js 264 行），所有"风险点"均已在代码中定位验证。*
