# 📋 雷霆战机 · 项目总结（供后续参考）

> 本文档汇总本项目截至目前的**全部已完成工作**、技术架构、部署信息与历史记录，供后续开发/维护直接参考。更新时间：2026-08。

---

## 一、项目概况

**名称**：雷霆战机 —— 2D 飞行射击网页小游戏（支持账号/好友/联机对战）

**形态**：全栈单仓
- 前端：`index.html`（UI 骨架 + 样式）+ `game.js`（游戏引擎，601 行）+ `net.js`（联机，113 行）+ `ui.js`（界面与社交，462 行）——Canvas 2D + Web Audio，零构建
- 后端：`server.js`（Node.js + Express + ws，约 300 行）
- 数据：运行时自动生成的 `data.json`（昵称/战绩/好友关系，密码以 scrypt 哈希存储，本地文件持久化）

**2026-09 更新**：原单文件 index.html（1450 行）已拆分为 game/net/ui 三模块 + UI 骨架；新增打击感四件套、连击 Combo、Boss 警示横幅、移动端触屏支持；后端新增密码哈希与战绩防刷；新增一键冒烟测试（`npm test`）

**代码量统计**：前端四文件共约 1180 行 + server.js 约 300 行

---

## 二、已完成功能清单（已核实）

### 游戏本体（index.html）
| 功能 | 说明 |
|------|------|
| 战机操控 | 鼠标 / 方向键 / WASD 移动，自动开火，空格或点击放炸弹 |
| 火力系统 | 1~5 级，弹幕形态递增（单发→双发→三发→散射→满级五弹道） |
| 道具系统 | ⚡P 火力升级 / ❤H 回血 / B 补炸弹 |
| 敌机体系 | 4 类：红小机(1血) / 紫中机(2血会射击) / 橙重机(4血蛇形) / Boss 战舰(扇形弹幕) |
| 动态难度 | 小兵血量随游戏时间增强（20s 后起步，120s +100%）；Boss 多波递增（首波 60 血 @30s，之后每 45s 一波 +70%，联机 ×1.5） |
| 视觉特效 | 星空滚动背景、尾焰/爆炸粒子、光晕、Boss 血条 |
| **打击感四件套** | 命中白闪、击杀顿帧 hit-stop（普通 30ms/Boss 90ms）、分级震屏（命中轻震/击杀中震/Boss 死亡重震/玩家受击重震）、得分飘字（+N 金字） |
| **连击 Combo** | 2 秒内连续击杀累计；≥8 得分 x2、≥20 得分 x3；击杀音效随连击升调；canvas 顶部 COMBO 计数 |
| **Boss 警示横幅** | Boss 出场前全屏 ⚠ WARNING ⚠ + "第 N 波 Boss 来袭"（闪烁动画） |
| **新手提示** | 开局前 5 秒底部操作提示（触屏/键盘文案自适应） |
| **移动端触屏** | 拖动屏幕移动（手指上方 70px 防遮挡）、双指点按/HUD 💣 按钮放炸弹、手机端 CSS 适配 |
| 音效 | Web Audio 合成：射击/爆炸/命中/拾取/炸弹/结束/入房 |
| **背景音乐** | 经典雷霆战机风格循环 BGM（Web Audio 实时合成，Am-F-C-G、BPM138、贝斯+鼓+主旋律），HUD 🎵 可开关 |
| **暂停/退出** | ESC 弹出暂停菜单（继续/退出）；HUD 退出按钮；中途退出也记录战绩 |
| 联机（房主权威） | 房主 P1 蓝/房客 P2 绿；共享生命值(联机5条)；房主 20Hz 同步状态快照；房客发操作指令 + 本地预测 |
| 联机音效同步 | 爆炸事件携带音效标签('hit'/'boom'/'power')随快照下发，房客端也能听到打击声 |
| **快照补全** | 快照携带 kills/combo，房客战绩击落数不再恒为 0（bug 修复），连击数同步显示 |

### 账号/社交/战绩（server.js + 前端）
| 接口/功能 | 说明 |
|------|------|
| 10 测试账户 | TF1001~TF1010，密码见下节；uid 固定唯一，昵称可自定义（已上线账户被改过名，如 TF1001→"YUE"，属服务器数据） |
| 登录/会话 | `/api/login` `/api/me` `/api/logout`，Bearer token，前端 localStorage 保持登录 |
| 战绩 | `/api/scores`：每局自动记录得分/击落数/模式/时间；主菜单显示近 5 局 |
| 排行榜 | `/api/leaderboard`：全员最高分 + 在线状态 |
| 好友 | `/api/friends`（增/删/列表）；实时在线状态（绿色圆点） |
| **邀请对战** | 好友在线 → 邀请 → 对方弹窗接受 → 房主自动建房并把 PeerJS 房间码经 WebSocket 下发 → 房客自动加入 |
| **开房间** | 房主生成 6 位房间码 + 复制邀请链接（`?room=码`，打开自动/登录后加入） |
| **输入房间码加入** | 主菜单输入框直接加入（已补，含错误提示） |
| WebSocket | `/ws?token=`：在线状态广播、邀请/接受/拒绝/房间码信令 |

### 已修复的历史 Bug（重要，勿回归）
1. 端口被占用 `EADDRINUSE` 直接崩溃 → 自动顺延端口 + 清晰提示（server.js）
2. 游戏中无法主动退出，只能等死亡 → ESC 暂停菜单 + HUD 退出按钮
3. 主菜单缺少"输入房间号加入"入口 → 补输入框 + `?room=` 自动加入
4. **联机房客无法移动** → 房客端 `initView()` 的玩家对象此前是普通对象缺 `applyInput` 方法致主循环崩溃，改为 `new Player(0)/new Player(1)` 并加防御
5. **联机无击打音效** → 快照 events 增加音效标签并下发
6. **房客战绩击落数恒为 0** → 快照增加 kills 字段，applySnapshot 同步（2026-09）

---

## 三、测试账户与密码

| UID | 默认昵称 | 密码 |
|------|---------|------|
| TF1001 | 猎鹰 | **710072**（已单独改） |
| TF1002~TF1010 | 闪电…战神 | 123456 |

- 登录框**不预填密码**（用户要求），需手动输入
- 密码在 server.js `PRESET_ACCOUNTS` 中按账户独立维护，启动时强制同步；**data.json 中仅存 scrypt 哈希**（`salt:hash`），不再有明文（2026-09 起）

---

## 四、部署信息（重要）

| 项 | 值 |
|----|----|
| **线上地址** | https://thunder-fighter.bonto.run |
| 托管平台 | Bonto（免费 50h/月、无需信用卡、支持 Node+WebSocket、30 分钟无访问自动休眠） |
| GitHub 仓库 | https://github.com/Yue2341/thunder-fighter（main 分支） |
| 本地运行 | `npm start` → http://localhost:3000 |
| 本地测试 | `npm test` → 前端桩测试 + 后端 API 冒烟（约 37 项断言） |
| 依赖 | express + ws（package.json，无新增依赖） |
| 线上状态 | 2026-09 已上线新版（拆分+玩法+安全加固），全链路验收通过 |

### 如何更新线上代码（Bonto）
> ⚠ 前端已拆分为 4 个文件（index.html / game.js / net.js / ui.js），**改动哪个文件就上传哪个**；首次部署需 4 个全部上传。上传前先 `npm test` 全绿。

```bash
cd D:/zyue/test
# 1) 检查会话（.bonto_session 存在且有效，否则重新授权）
# 2) 上传单个文件（以 game.js 为例，用 upload_file 拿一次性 URL 再 POST；其余文件同理）
SID=$(cat .bonto_session)
URL=$(curl -s -m 60 -X POST https://api.bonto.dev/mcp \
  -H "Content-Type: application/json" -H "Accept: application/json, text/event-stream" -H "Mcp-Session-Id: $SID" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"upload_file","arguments":{"app":"thunder-fighter","path":"game.js"}}}' \
  | grep '^data:' | sed 's/^data: //' | python -c "import sys,json,re;print(re.search(r'https://[^\"\\\\ ]+',json.loads(sys.stdin.read())['result']['content'][0]['text']).group(0))")
curl -F "file=@game.js" "$URL"
# 3) 等 ~12s 自动重启后验证
```
- 认证令牌存于 `bonto_auth.json`（JWT，2026-08 生成，有效期约 90 天）；MCP 会话在 `.bonto_session`——两者均已加入 `.gitignore`，勿提交
- `bonto.sh <tool> '<json-args>'` 为封装好的调用脚本（tools/call）；大文件请用 upload_file（Windows 命令行约 32KB 参数上限，write_file 对大文件会失败）

### 其它平台备用
- `render.yaml`：Render Blueprint（**注意**：Render 新账号需绑信用卡验证，2026 现状）
- `Dockerfile`：Railway/Fly 等 Docker 平台可用

---

## 五、技术要点备忘

- **联机架构**：PeerJS(0.peerjs.com 云信令) 做 P2P 游戏数据；自有后端只做 账号/好友/在线/邀请信令/战绩。房主权威模拟，房客"发指令+本地预测渲染"。
- **快照同步**（host→guest，20Hz）：players/enemies/eBullets/bullets/powerups/score/lives/kills/combo/events；events 形如 `[x,y,color,n,sound?]`。
- **打击感实现**：分级震屏 `addShake(v)`（取最大值不叠加）+ `hitStop` 顿帧（冻结世界仅粒子/飘字衰减）+ `Enemy.flash` 白闪（0.07s）+ `game.texts` 飘字（0.8s 上浮）。
- **连击实现**：`killReward(e)` 统一击杀奖励入口（普通击杀与炸弹击杀共用），`comboMult()` 倍率，`sfx.boom(combo)` 升调。
- **触屏实现**：touchmove 更新 mouse 坐标（上偏 70px），双指 touchstart 触发 `triggerBomb()`；`IS_TOUCH` 检测显示 HUD 💣 按钮。
- **密码存储**：scrypt 哈希（`hashPw/verifyPw`，crypto 内置，无新依赖）；预设账户启动时强制重哈希同步；旧明文字段启动时自动迁移删除。
- **战绩防刷**：POST /api/scores 校验 `score ≤ kills×23000 + 20000`（单杀最高分 Boss 7500×连击 x3 也到不了），明显伪造直接 400。
- **会话**：无状态 JWT（HMAC-SHA256，30 天有效，密钥存 data.json 首次生成），服务器重启不掉登录态。
- **⚠ Bonto 部署教训（重要）**：Bonto 用 nodemon 监视 app 目录，**运行时写 app 根目录的任何文件都会触发服务器重启**（旧版每局结束写 data.json 就重启一次、token 全丢）。现状：数据文件在 `node_modules/.thunderdata/data.json`（nodemon 默认忽略 node_modules，仍在持久 volume 内，重启后数据完好）；启动时仅在密码迁移/首次生成 JWT 密钥时落盘一次。若改数据存储逻辑，务必确认写入路径不被 nodemon 监视。
- **BGM 合成**：Web Audio 音序器 `scheduleBGM()`（lookahead 0.12s 调度，setInterval 25ms 推进），`startBGM/stopBGM/toggleBGM`；与 `sfx` 共用 `actx`。
- **难度曲线**：小兵 `timeBonus = min(1, max(0, (time-20)/100))`；Boss `nextBossTime = time+45`，`wave = floor(time/45)+1`，`hp = round(60*(1+(wave-1)*0.7))`（联机 ×1.5）。Boss 死亡/被炸弹击杀后重置 `bossSpawned=false`。
- **暂停系统**：`setPaused(v)` 显示/隐藏 `#pauseMenu`；输入侧对 bomb 已加 `!paused` 守卫。

---

## 六、文件清单

| 文件 | 作用 |
|------|------|
| `index.html` | UI 骨架 + 样式 + 脚本引用（登录/菜单/好友/房间/暂停/结算） |
| `game.js` | 游戏引擎：音频/BGM、实体类、游戏逻辑、打击感反馈、连击、渲染、输入（含触屏） |
| `net.js` | 联机：PeerJS P2P 房主权威快照、WebSocket 信令 |
| `ui.js` | 界面与社交：登录/菜单/好友/战绩/邀请流程/启动入口（最后加载） |
| `server.js` | Node 后端：账号（scrypt 哈希）/好友/战绩（防刷）/在线/邀请信令 + 静态托管 |
| `test.sh` | 一键冒烟测试（前端桩测试 + 后端 API 全链路） |
| `scripts/stub-test.js` | 无浏览器桩测试：Node vm 模拟环境跑完整游戏逻辑并断言 |
| `package.json` / `package-lock.json` | 依赖（express、ws）与启动/测试脚本 |
| `data.json` | 运行时数据（密码仅存哈希；已被 .gitignore，勿提交） |
| `README.md` / `使用说明.md` | 项目说明与玩家使用指南 |
| `趣味性优化建议.md` / `项目总结.md` / `项目分析与优化建议-复审.md` | 改造建议 / 项目总结 / 独立复审报告 |
| `render.yaml` / `Dockerfile` | 备用部署配置 |
| `bonto.sh` | Bonto MCP 管理脚本 |
| `bonto_auth.json` / `.bonto_session` / `.bonto_headers` | Bonto 认证与会话（.gitignore，勿提交/勿泄露） |
| `.gitignore` | 忽略 node_modules/data.json/.tmp/日志/凭据 |

---

## 七、后续方向指引

- 趣味化/玩法增强的具体建议（关卡、局外养成等）见 **《趣味性优化建议.md》**；独立复审与优先级重排见 **《项目分析与优化建议-复审.md》**
- **已落地**（2026-09）：打击感四件套、连击 Combo、Boss 警示、新手提示、触屏支持、文件拆分、密码哈希、战绩防刷、冒烟测试
- **待做**（按复审优先级）：星级评价/关卡进度、金币+局外商店（建议先"每日首胜"探路）、断线重连、弱网快照优化（只发最新帧）、周榜/邀请奖励
- 按建议实现新系统时，后端扩展集中在 `server.js` 的 API 区；前端引擎改动在 `game.js`、联机在 `net.js`、界面在 `ui.js`；完成后 `npm test` 全绿再按第四节流程经 Bonto 更新
